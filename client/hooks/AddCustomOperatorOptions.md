# AddCustomOperatorOptions

Runs when the Operator menu is created, allows for adding more options to the operator menu

## Arguments
`DPanel operator` The operator panel

`DForm secondform` The operator form
